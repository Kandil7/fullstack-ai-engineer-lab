# Normalize line endings — keep LF in the repo, let Git handle checkout.
* text=auto eol=lf

# Scripts that must stay CRLF / LF respectively
*.ps1 text eol=crlf
*.sh  text eol=lf

# Binary
*.png binary
*.jpg binary
*.pdf binary
