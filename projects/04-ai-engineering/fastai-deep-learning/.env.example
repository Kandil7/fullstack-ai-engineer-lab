# fast.ai Deep Learning track — environment template
# Copy to .env and fill in. .env is gitignored at the repo root.

# Optional: only needed for the deployment lesson (Hugging Face Spaces push)
HUGGINGFACE_TOKEN=

# Optional: Kaggle API creds for pulling course datasets locally
KAGGLE_USERNAME=
KAGGLE_KEY=
