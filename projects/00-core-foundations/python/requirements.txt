# =============================================================================
# Fullstack AI Engineer Lab — Python Module Requirements
# =============================================================================
# Install all at once: pip install -r requirements.txt
# 
# Organized by module/phase. Uncomment only what you need.
# =============================================================================

# ── Phase 1: Core Python ─────────────────────────────────────────────────────
# No external dependencies required (pure Python standard library)

# ── Phase 2: Advanced Python ─────────────────────────────────────────────────
# No external dependencies required (pure Python standard library)

# ── Phase 3: Data Science Libraries ──────────────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
scipy>=1.11.0
openpyxl>=3.1.0        # pandas read_excel / to_excel
seaborn>=0.13.0        # pandas visualization case studies
statsmodels>=0.14.0    # time-series case study (decomposition, ADF)
pyarrow>=15.0.0        # pandas to_parquet / Arrow interop

# ── Phase 4: Databases ───────────────────────────────────────────────────────
# MySQL uses built-in sqlite3 (no install needed)
# MongoDB uses dict stand-ins (no install needed)
# For real MongoDB use: pymongo>=4.5.0
# For real MySQL use: mysql-connector-python>=8.2.0

# ── Phase 5: Web Frameworks ──────────────────────────────────────────────────
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
httpx>=0.25.0
jinja2>=3.1.0
aiofiles>=23.2.0
sqlalchemy>=2.0.0
pydantic[email]>=2.0.0
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Django (REFERENCE-ONLY - not installed by default, excluded from smoke tests)
# The django/ folder holds reference snippets; install to run them:
# django>=5.0.0
# djangorestframework>=3.14.0
# djangorestframework-simplejwt>=5.3.0

# ── Phase 6: Data Structures & Algorithms ────────────────────────────────────
# No external dependencies required (pure Python standard library)

# ── Phase 7: Machine Learning ────────────────────────────────────────────────
scikit-learn>=1.3.0

# ── Phase 8: MLOps ────────────────────────────────────────────────────────────
# Core exercises run with the stdlib + pandas/sklearn (mock registries, hash
# tables, and in-memory stores). These tools are used in the lectures and are
# optional for the exercises themselves:
mlflow>=2.9.0            # experiment tracking + model registry (02, 04)
pandera>=0.18.0          # data validation schemas (10)
dvc>=3.0.0               # data versioning (03) — CLI, not imported
pydantic>=2.0.0          # validation + signatures (04, 05, 10)
# Airflow/Prefect are orchestration platforms — run as separate services:
# prefect>=2.0.0
# apache-airflow>=2.8.0
# Serving frameworks (optional, referenced in lectures 06-08):
# tritonclient
# onnxruntime>=1.16.0    # inference optimization (08)

# ── Phase 9: GenAI ────────────────────────────────────────────────────────────
# All 25 exercises are deterministic and run offline (mock LLMs, mock
# embeddings — no API keys required). Install these to follow the lectures and
# connect real providers:
tiktoken>=0.6.0          # token counting (01)
openai>=1.30.0           # OpenAI API client (02, 03, 06)
anthropic>=0.26.0        # Anthropic API client (02)
sentence-transformers>=2.6.0  # local embeddings + rerankers (06, 12, 22)
# vLLM / Ollama are local serving engines — installed separately:
# vllm
# ollama
